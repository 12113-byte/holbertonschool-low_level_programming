#include "main.h"
#include <stdio.h>

/**
 * main - changes the file name
 * @argc: voided
 * @argv: points to file name
 * Return: 0 on success
 *
 */

int main(int argc, char *argv[])
{
void(argc);
printf("%s\n", argv[0]);
return (0);
}

